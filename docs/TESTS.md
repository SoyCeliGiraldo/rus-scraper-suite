# Pruebas

- Unitarias y de integración con Jest y Supertest.
- Adapters de parsing validados con fixtures HTML.

## Comandos
- `npm test` — todas las pruebas.
- `npm run test:integration` — sólo integración.

## Cobertura
Se muestra al final del `npm test`.
